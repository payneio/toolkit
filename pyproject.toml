[project]
name = "myscripts"
version = "0.1.0"
requires-python = ">=3.8"
dependencies = [
  "requests>=2.28.0",
  "beautifulsoup4>=4.11.0",
]

[project.optional-dependencies]
dev = [
  "pytest>=7.0.0",
  "black>=22.0.0",
]

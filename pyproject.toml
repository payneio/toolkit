[project]
name = "toolkit"
version = "0.1.0"
requires-python = ">=3.8"
dependencies = [
  "requests>=2.28.0",
  "beautifulsoup4>=4.11.0",
  "openai>=1.6.0",
  "toml>=0.10.2",
]

[project.optional-dependencies]
dev = [
  "pytest>=7.0.0",
  "ruff>=0.11.4",
]
